import { describe, it, expect, beforeEach, afterEach } from 'vitest' import { createFixtureManager }
from '@cli-ops/shared-testing' import
{{pascalCase topic}}{{pascalCase name}}
from '../../../src/commands/{{topic}}/{{name}}.js' describe('{{topic}}:{{name}}', () => { const
fixtures = createFixtureManager() beforeEach(async () => { // TODO: Set up test fixtures // Example:
await fixtures.create({ 'config.json': '{}' }) }) afterEach(async () => { await fixtures.cleanup()
}) it('should execute successfully', async () => { // TODO: Implement test expect(true).toBe(true)
}) it('should handle errors gracefully', async () => { // TODO: Implement error handling test
expect(true).toBe(true) }) })