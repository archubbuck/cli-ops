name: Release
# This workflow uses npm Trusted Publishing with OIDC for secure, token-less authentication
# - No NPM_TOKEN required - authentication via OIDC (id-token: write permission)
# - Automatic provenance attestation for supply chain security
# - Requires: Trusted publishers configured on npmjs.com for each package
# - See: docs/NPM-TRUSTED-PUBLISHING.md for setup instructions

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - name: Validate RELEASE_TOKEN
        if: ${{ !env.RELEASE_TOKEN }}
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          echo "::error::RELEASE_TOKEN secret is required for the release workflow"
          echo "::error::The default GITHUB_TOKEN cannot create pull requests"
          echo "::error::Please follow the setup instructions in docs/RELEASE-PROCESS.md"
          echo "::error::See https://github.com/${{ github.repository }}/blob/${{ github.event.repository.default_branch }}/docs/RELEASE-PROCESS.md"
          exit 1

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN }}

      - uses: ./.github/actions/setup-workspace
        with:
          registry-url: 'https://registry.npmjs.org'

      - name: Build
        run: pnpm build

      - name: Create Release Pull Request or Publish
        id: changesets
        uses: changesets/action@v1
        with:
          publish: pnpm changeset:publish
          version: pnpm changeset:version
          commit: 'chore: Version packages'
          title: 'chore: Version packages'
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

      - name: Publish provenance
        if: steps.changesets.outputs.published == 'true'
        run: |
          echo "Packages published with provenance via OIDC Trusted Publishing:"
          echo "${{ steps.changesets.outputs.publishedPackages }}"
          echo ""
          echo "Provenance attestation automatically included with npm v9.5.0+"
